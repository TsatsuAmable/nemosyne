name: reconcile-rf035b-roadmap

on:
  push:
    branches:
      - docs/roadmap-reconcile-rf035b

permissions:
  contents: write

jobs:
  reconcile:
    runs-on: ubuntu-latest
    steps:
      - name: Sanity check
        run: echo reconciliation-runner-ready
