name: P1-R2E B4 relationship graph browser evidence

on:
  pull_request:
    branches: [main]
    paths:
      - '.github/workflows/p1r-graph-b4-evidence.yml'
      - 'src/app/graphEvidenceDiagnostics.ts'
      - 'src/app/bootstrap.ts'
      - 'src/app/dataset/LoadDatasetUseCase.ts'
      - 'src/app/dataset/SemanticEmbodimentLoader.ts'
      - 'src/app/dataset/SemanticDetailTransition.ts'
      - 'src/atlas/ports/analytical.worker.ts'
      - 'src/data/Dataset.ts'
      - 'src/moneta/MonetaTopologyNode.ts'
      - 'src/moneta/VRTopologyTranslator.ts'
      - 'src/moneta/embodiment/GraphSemanticEmbodiment.ts'
      - 'src/moneta/embodiment/SemanticEmbodimentStatus.ts'
      - 'src/moneta/representation/GraphEmbodimentPayload.ts'
      - 'src/moneta/representation/MonetaHypothesisEngine.ts'
      - 'src/moneta/representation/RelationshipGraphAuthority.ts'
      - 'src/moneta/representation/RepresentationCandidate.ts'
      - 'src/moneta/representation/RepresentationRequirements.ts'
      - 'src/moneta/representation/SemanticDrillDown.ts'
      - 'src/vr/presentation/representation/RepresentationSurface.ts'
      - 'src/wasm/LayoutAuthorityBridge.ts'
      - 'src/wasm/runtime/SemanticEmbodimentBridge.ts'
      - 'wasm/src/moneta/graph_embodiment.rs'
      - 'wasm/src/moneta/drill_down.rs'
      - 'wasm/src/moneta/embodiment.rs'
      - 'tests/smoke/p1r-graph-b4-evidence.spec.ts'
      - 'tests/p1r-r2e-b4-evidence-trigger-coverage.test.ts'
      - 'docs/roadmap/P1_R2E_RELATIONSHIP_GRAPH.md'
      - 'docs/ROADMAP.md'
      - 'docs/review/P1_R2E_B4_STOP_REVIEW_2026-09-01.md'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: p1r-graph-b4-evidence-${{ github.ref }}
  cancel-in-progress: true

jobs:
  evidence:
    name: Visible graph + scale + perceptual evidence
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      NEMOSYNE_SOURCE_HEAD_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
    steps:
      - name: Checkout exact evidence head
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}

      - name: Verify exact checkout identity
        shell: bash
        run: |
          set -euo pipefail
          checkout_sha="$(git rev-parse HEAD)"
          if [[ "$checkout_sha" != "$NEMOSYNE_SOURCE_HEAD_SHA" ]]; then
            echo "Expected checkout $NEMOSYNE_SOURCE_HEAD_SHA but found $checkout_sha" >&2
            exit 1
          fi
          echo "NEMOSYNE_CHECKOUT_HEAD_SHA=$checkout_sha" >> "$GITHUB_ENV"
          echo "NEMOSYNE_WORKFLOW_EVENT_SHA=$GITHUB_SHA" >> "$GITHUB_ENV"

      - name: Set up Node.js 24
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build instrumented production bundle
        run: |
          mkdir -p p1r-graph-b4-results
          /usr/bin/time -v -o p1r-graph-b4-results/build-time.txt \
            env \
              VITE_NEMOSYNE_DIAGNOSTICS=1 \
              VITE_NEMOSYNE_GRAPH_B4_EVIDENCE=1 \
              VITE_NEMOSYNE_Q3B_RESOURCE_PROBE=1 \
              npm run build

      - name: Pin evidence artifact identities
        shell: bash
        run: |
          set -euo pipefail
          bundle_sha="$({ find dist -type f -print0 | LC_ALL=C sort -z | xargs -0 sha256sum; } | sha256sum | awk '{print $1}')"
          wasm_sha="$(sha256sum dist/wasm/pkg/nemosyne_wasm_bg.wasm | awk '{print $1}')"
          echo "NEMOSYNE_GRAPH_B4_BUNDLE_SHA256=$bundle_sha" >> "$GITHUB_ENV"
          echo "NEMOSYNE_GRAPH_B4_WASM_SHA256=$wasm_sha" >> "$GITHUB_ENV"
          printf 'source_head_sha=%s\ncheckout_head_sha=%s\nworkflow_event_sha=%s\nbundle_sha256=%s\nwasm_sha256=%s\n' \
            "$NEMOSYNE_SOURCE_HEAD_SHA" \
            "$NEMOSYNE_CHECKOUT_HEAD_SHA" \
            "$NEMOSYNE_WORKFLOW_EVENT_SHA" \
            "$bundle_sha" \
            "$wasm_sha" \
            | tee p1r-graph-b4-results/artifact-identities.txt

      - name: Install Chromium
        run: npx playwright install --with-deps chromium

      - name: Capture relationship graph B4 production evidence
        run: |
          /usr/bin/time -v -o p1r-graph-b4-results/probe-time.txt \
            env \
              NEMOSYNE_GRAPH_B4_BROWSER_PROBE=1 \
              NEMOSYNE_SMOKE_PREBUILT=1 \
              npx playwright test tests/smoke/p1r-graph-b4-evidence.spec.ts --retries=0

      - name: Upload B4 structured and visual evidence
        if: ${{ always() }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: p1r-graph-b4-evidence
          path: p1r-graph-b4-results
          if-no-files-found: warn
          retention-days: 7
