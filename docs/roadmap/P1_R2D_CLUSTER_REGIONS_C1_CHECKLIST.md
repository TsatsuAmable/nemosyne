# P1-R2D C1 Checklist

C1 may begin only from fresh `main` after the R2D rail merges.

Required decisions before production code:

- exact source-authoritative partition declaration;
- exact 2D/3D coordinate-field contract;
- missing/unassigned/coordinate-invalid accounting;
- zero-coordinate-valid cluster wire state;
- stable scalar partition canonicalization and semantic-ID derivation;
- reviewed hard cluster bound;
- strict analytical method name/version/parameters;
- exact information contract and any new information type;
- quantified bootstrap rank effects from ontology corrections;
- new fitness model/treatment identity if ranking changes;
- malformed request/payload and row-smuggling falsifiers;
- explicit proof that C1 does not make `CLUSTER_REGIONS` production-valid.
