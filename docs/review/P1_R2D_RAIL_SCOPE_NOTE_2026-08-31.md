# P1-R2D rail scope note - 31 August 2026

This branch is intentionally documentation/governance only. It does not change runtime code, ranking, Rust/WASM ABI, Worker execution, Three.js rendering, or production availability of `CLUSTER_REGIONS`.

The first runtime checkpoint is C1 on a fresh branch after this rail merges. C1 owns the exact request/payload schema, ontology reconciliation, hard bounds, treatment-version decision, and falsifiers. Production cutover remains C3.
