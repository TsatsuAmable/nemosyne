import {describe,expect,it} from 'vitest'; import {parsePortableExperimentConfig,buildPortableExperimentBundle} from '../../dev/xr-lab/PortableXRExperiment.ts';
const base={schemaVersion:1,protocolId:'p',protocolVersion:'1',architectures:['SWSE_BASELINE','COUPLED_STAIRCASE','ORTHOGONAL_MATRIX'],dataset:{id:'d',fingerprint:'fp',oracleId:'o'},replayTraceId:'t',seeds:[1],resourceBudgetId:'b',perturbations:{data:{enabled:true,runs:12,magnitude:0.12},runtime:{scenarios:['teleport-flood','cancellation-storm','memory-pressure']},experimental:{counterbalanceArmOrder:true}},evidence:{rawDirectory:'raw',derivedDirectory:'derived',allowHumanClaimsFromSimulator:false,allowDeviceQualificationFromSimulator:false}};
describe('portable perturbation config',()=>{it('binds perturbation policy into content addressed bundle',()=>{const c=parsePortableExperimentConfig(base);const b=buildPortableExperimentBundle(c,'h');expect(b.config.perturbations!.data.runs).toBe(12);});it('fails closed on invalid perturbation magnitude',()=>expect(()=>parsePortableExperimentConfig({...base,perturbations:{...base.perturbations,data:{enabled:true,runs:12,magnitude:-1}}})).toThrow(/perturbation/i));it('requires deterministic counterbalancing',()=>expect(()=>parsePortableExperimentConfig({...base,perturbations:{...base.perturbations,experimental:{counterbalanceArmOrder:false}}})).toThrow(/counterbalance/i));});
